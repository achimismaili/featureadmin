<UserControl x:Class="FeatureAdmin.Views.DetailView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			  xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:FeatureAdmin.Views"
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="300">
	<StackPanel>
		<Label x:Name="Title"
			HorizontalAlignment="Center"
				   FontWeight="Bold"
			   />
		<DataGrid x:Name="Items"
				  Margin="5"
				  AutoGenerateColumns="False"
				  CanUserAddRows="False">
			<DataGrid.Columns>
				<DataGridTextColumn Binding="{Binding Key,Mode=OneWay}" Header="Keys" />
				<DataGridTextColumn Binding="{Binding Value,Mode=OneWay}" Header="Values" />
			</DataGrid.Columns>
		</DataGrid>
	</StackPanel>
</UserControl>
