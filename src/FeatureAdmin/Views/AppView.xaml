<UserControl x:Class="FeatureAdmin.Views.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:FeatureAdmin.Views"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <local:NavigationBarView 
            x:Name="navigationBar"
            Grid.Row="0"
            ></local:NavigationBarView>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>

            <!-- List Row-->
            <ContentControl cal:View.Model="{Binding LocationList}"
            Grid.Row="0" Grid.Column="0" />
            <local:FeatureDefinitionListView 
            x:Name="featureDefinitionList"
            Grid.Row="0" Grid.Column="1"
            ></local:FeatureDefinitionListView>
            <local:LogView 
            x:Name="log"
            Grid.Row="0" Grid.Column="3"
            ></local:LogView>

            <!--Detail Row-->
            <local:LocationView 
            x:Name="location"
            Grid.Row="1" Grid.Column="0"
            ></local:LocationView>
            <local:FeatureDefinitionView 
            x:Name="featureDefinition"
            Grid.Row="1" Grid.Column="1"
            ></local:FeatureDefinitionView>
            <local:CommandView 
            x:Name="command"
            Grid.Row="1" Grid.Column="3"
            ></local:CommandView>
            <TextBox x:Name="Maus"
                Grid.Row="2" Grid.Column="0"
           Grid.ColumnSpan="3"
                      />
        </Grid>
        <local:StatusBarView 
            x:Name="statusBar"
            Grid.Row="2"
            ></local:StatusBarView>
    </Grid>
</UserControl>
