<UserControl x:Class="FeatureAdmin.Views.LocationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:FeatureAdmin.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Grid.Row="0">
                <Label Content="Search:" />
                <TextBox Text="{Binding SearchInput, UpdateSourceTrigger=PropertyChanged}"
                         Width="150" />
                <Button Content="X"
                        x:Name="ClearSearchCommand" />
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Grid.Row="1">
                <TextBlock Text="Scope Filter:" />
                <RadioButton GroupName="ScopeFilter" x:Name="ScopeFilterAll" >
                    <TextBlock Text="All" />
                </RadioButton>
                <RadioButton GroupName="ScopeFilter" x:Name="ScopeFilterFarm" >
                    <TextBlock Text="F" />
                </RadioButton>
                <RadioButton GroupName="ScopeFilter" x:Name="ScopeFilterWebApp" >
                    <TextBlock Text="WA" />
                </RadioButton>
                <RadioButton GroupName="ScopeFilter" x:Name="ScopeFilterSite" >
                    <TextBlock Text="S" />
                </RadioButton>
                <RadioButton GroupName="ScopeFilter" x:Name="ScopeFilterWeb" >
                    <TextBlock Text="W" />
                </RadioButton>
            </StackPanel>
        </Grid>
        <DataGrid x:Name="Locations"
            Grid.Row="1"/>
    </Grid>
</UserControl>
