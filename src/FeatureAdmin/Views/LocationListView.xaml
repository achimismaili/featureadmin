<UserControl x:Class="FeatureAdmin.Views.LocationListView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
			 xmlns:cal="http://www.caliburnproject.org"
			 xmlns:local="clr-namespace:FeatureAdmin.Views"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Label Content="Search:"
                   HorizontalAlignment="Left"
                   Margin="5,2"
				   Grid.Row="0" Grid.Column="0" />
			<TextBox x:Name="SearchInput"
					controls:TextBoxHelper.ClearTextButton="True"
					Margin="5,2"
                     Grid.Row="0" Grid.Column="1" />
			<Label Content="Guid filter:"
                   HorizontalAlignment="Left"
                   Margin="5,2"
				   Grid.Row="1" Grid.Column="0" />
			<TextBox x:Name="IdFilter"
						 controls:TextBoxHelper.ClearTextButton="True"
					 controls:TextBoxHelper.Watermark="Filter for Location, parent or active feature"
					 Margin="5,2"
                     Grid.Row="1" Grid.Column="1" />
			<Label Content="Scope Filter:"
                   HorizontalAlignment="Left"
                       Margin="5,2"
						   Grid.Row="2" Grid.Column="0" />
            <ComboBox x:Name="ScopeFilters"
				Margin="5,2"
				IsEditable="True"
				controls:TextBoxHelper.ClearTextButton="True"
				Grid.Row="2" Grid.Column="1">
            </ComboBox>
		</Grid>
        <DataGrid x:Name="Locations"
                  Margin="5"
                  AutoGenerateColumns="False"
			Grid.Row="1"
		    CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DisplayName}" Header="Title" />
                <DataGridTextColumn Binding="{Binding Scope}" Header="Scope" />
                <DataGridTextColumn Binding="{Binding Url}" Header="Url" />
            </DataGrid.Columns>
        </DataGrid>
	</Grid>
</UserControl>