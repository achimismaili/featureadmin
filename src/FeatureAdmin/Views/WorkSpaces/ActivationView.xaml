<UserControl x:Class="FeatureAdmin.Views.WorkSpaces.ActivationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:FeatureAdmin.Views.WorkSpaces"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Location -->
        <ContentControl cal:View.Model="{Binding LocationListVm}"
            Grid.Column="0" />
  
        <GridSplitter Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" 
                          VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch"/>

        <!-- Feature -->
        <ContentControl cal:View.Model="{Binding FeatureDefinitionListVm}"
           Grid.Row="1" Grid.Column="2"
            ></ContentControl>
    </Grid>
</UserControl>
